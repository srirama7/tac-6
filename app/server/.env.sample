# API Keys for LLM providers
# You need at least one of these to use the natural language to SQL feature

# Google Gemini API Key (primary provider - used for OpenAI-compatible operations)
GEMINI_API_KEY=your-gemini-api-key-here

# Claude Code CLI Path (uses local authentication, no API key needed)
# Default is "claude" but you can specify full path
CLAUDE_CODE_PATH=claude
